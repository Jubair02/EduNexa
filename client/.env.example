# In development, /api is proxied to the Express server by Vite (vite.config.ts),
# so this works no matter which port Vite runs on.
# For a deployed client, set the full API origin, e.g. https://api.example.com/api
VITE_API_URL=/api
